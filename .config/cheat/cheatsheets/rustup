# rust-cheat-sheet based on
# https://zerotomastery.io/cheatsheets/rust-cheat-sheet/
#########################################################

# show current installed & active toolchains
rustup show

# update all toolchains
rustup update

# set the default toolchain
rustup default $TOOLCHAIN

# list available components
rustup component list

# add a component (like Clippy or offline docs)
rustup component add $NAME

# list available compilation targets
rustup target list

# add a compilation target
rustup target add $NAME

