#!/usr/bin/env bash

git diff --staged \
  | claude \
    --permission-mode dontAsk \
    --max-turns 1 \
    --tools "" \
    --print \
    --system-prompt-file ~/.config/ai-roles/committer.md \
    "Generate commit message, my changes:"
