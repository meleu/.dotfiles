#!/bin/sh

# load private aliases
if [ -f "${HOME}/.aliases_private" ]; then
  . "${HOME}/.aliases_private"
fi

alias vim='nvim'

# use vim with a bare minimum configuration
alias vi='nvim --clean'

alias n='vi ~/notes.md'

# neovide
alias v='neovide --frame none'

alias dls='docker container ls'

# inspired by <https://www.atlassian.com/git/tutorials/dotfiles>
# gdf stands for "git dotfiles"
alias gdf='git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

# prevent typo with `cheat`
alias chat='echo "You mean \"cheat\", right?"'

# allow aliases to be sudoed
alias sudo='sudo '

###############################################################################
# `getclip` - spits the clipboard on stdout
# `setclip < ${file}` - puts file contents in the clipboard
# `command | setclip` - puts command's output in the clipboard

: "${OSTYPE:=$(uname -s)}"
case "$OSTYPE" in
  "darwin"*) # MacOS
    alias getclip='pbcopy'
    alias setclip='pbpaste'
    ;;
  *) # Linux (hopefully)
    alias getclip='xclip -selection clipboard -o'
    alias setclip='xclip -selection c'
    ;;
esac

###############################################################################

# easier navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias -- -='cd -'

# Interactive operation...
alias rm='rm -iv'
alias cp='cp -iv'
alias mv='mv -iv'

# Default to human readable figures
alias df='df -h'
alias du='du -h'

# "disk usage here": disk usage of current directory
alias duh='du -cksh'

alias less='less -r'   # raw control characters
alias whence='type -a' # where, of a sort
alias grep='grep --color'
alias egrep='grep -E --color=auto'
alias fgrep='grep -F --color=auto'

# Some shortcuts for different directory listings
alias ls='ls -hF --color=tty'

###############################################################################
# misc stuff
###############################################################################

# print each dir in PATH on a separate line
# NOTE: this doesn't work as expected in zsh
# alias path='echo -e "${PATH//:/\\n}" | sort'

# eza - successor of exa ('ls' replacement)
alias eza='eza -F'
alias l='eza --color=always --long --git --no-filesize --icons=always --no-time --no-user --no-permissions --group-directories-first --sort extension'
alias lt='l --tree'
alias ll='eza --long --group-directories-first --sort extension'
alias lla='ll -a'
alias lsd='eza -D' # list directories only

# show my IP address that is going to the internet
alias myip='curl -s4 icanhazip.com'
alias myip6='curl -s6 icanhazip.com'

# GCP stuff
# alias gcpProject='gcloud config get-value project'

# always use docker compose plugin instead of standalone docker-compose
alias docker-compose='docker compose'

# lazygit is a cool TUI for git
alias lgit='lazygit'

# lazydocker is a cool TUI for docker stats
alias ldocker='lazydocker'

# https://github.com/firecow/gitlab-ci-local
alias glci='gitlab-ci-local'

# sync current branch with main
alias mergemain='git checkout main && git pull && git checkout - && git pull && git merge main'

# update kitty:
# https://sw.kovidgoyal.net/kitty/binary/
alias kitty-update='curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin'

# shell gpt custom roles
# https://github.com/TheR1D/shell_gpt?tab=readme-ov-file#roles
# alias sgptgit='sgpt --role git-master'
# alias sgptrb='sgpt --role ruby-master'
# alias sgptdocker='sgpt --role docker-master'
# alias sgptbash='sgpt --role bash-master'
# alias sgptaws='sgpt --role aws-master'
# alias sgptjq='sgpt --role jq-master'
# alias sgptglab='sgpt --role gitlab-ci-master'
#
# trying mods instead of Shell GPT
alias ai=mods
alias ai-sh='mods --role shell'
alias ai-aws='mods --role aws'
alias ai-bash='mods --role bash'
alias ai-docker='mods --role docker'
alias ai-git='mods --role git'
alias ai-glab='mods --role gitlab'
alias ai-jq='mods --role jq'
alias ai-rb='mods --role ruby'
alias ai-go='mods --role golang'
alias ai-net='mods --role networking'
